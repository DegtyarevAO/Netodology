locals {
  vm_name_worker1 = "${var.project}-${var.contur}-${var.role1}"
  vm_name_worker2 = "${var.project}-${var.contur}-${var.role2}"
  vm_name_arbitr = "${var.project}-${var.contur}-${var.role3}"
